 "use treat"; // treat all JS code as newer version.

 // alert(3 + 3) // we are using node js, not browser.

 console.log(3 + 3)
// code readability should be high. 

console.log("shivam")

// JavaScript has 8 data types. 
let name = "shivam"         // 1. string
let age = 24                // 2. number
                            // 3. bigint
let isLoggedIn = false;     // 4. boolean (true)
let state;                  // 5. null
                            // 6. undefined
                            // 7. symbol
                            
                            // 8. object
 
/* an object data type can contain 1. An Object
                                   2. An Array
                                   3. A Date
*/

//object
const person = {
    firstName: "Shivam",
    LastName: "Singh"
}

//Array object
const cars = ["mercedes", "BMW", "ferrari"]

//Date Object
const dates = new dates("2022-03-25")


console.log(typeof undefined); //undefined
console.log(typeof null);      //object
